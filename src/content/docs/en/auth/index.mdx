---
title: Authentication
description: "Add password hashing, login, and JWT sessions so the admin belongs to real users."
sidebar:
  order: 0
---

Add password hashing, login, and JWT sessions so the admin belongs to real users.

Start with [Passwords →](/devblog/en/auth/passwords/), then work through the lessons in order (see the sidebar).
