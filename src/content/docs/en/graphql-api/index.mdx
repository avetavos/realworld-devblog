---
title: GraphQL API
description: "Serve posts and tags through a code-first GraphQL API with resolvers, DTOs, and schema generation."
sidebar:
  order: 0
---

Serve posts and tags through a code-first GraphQL API, with resolvers, DTOs, and automatic schema generation.

Start with [Code-first basics →](/devblog/en/graphql-api/code-first-basics/), then work through the lessons in order (see the sidebar).
