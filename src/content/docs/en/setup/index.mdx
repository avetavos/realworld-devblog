---
title: Setup & Tooling
description: "Set up the monorepo, the NestJS API, the Next.js app, and a first Docker Compose file running MongoDB."
sidebar:
  order: 0
---

Set up the monorepo, the NestJS API, the Next.js app, and a first Docker Compose file running MongoDB.

Start with [Repo layout →](/devblog/en/setup/repo-layout/), then work through the lessons in order (see the sidebar).
